1. Install ruby
2. clone it and do cd app
3. run bundle install (if bundle is not there install bundler first)
4. run bundle exec rails console
5. app console is up and running :)
6. to set key val in cache use - Cache.set_val_in_cache(key, val)
7. to retrieve val corresponding to a key use - Cache.get_val_from_cache(key)
8. It is thread safe and to operate in multithreading it uses connection pool (here it is initialized with pool size 10)